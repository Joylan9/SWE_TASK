// ── App.tsx ──────────────────────────────────────────────

import { WallCalendar } from './components/WallCalendar';

/**
 * Root application component.
 * Renders the WallCalendar as the main content.
 */
function App() {
  return <WallCalendar />;
}

export default App;
